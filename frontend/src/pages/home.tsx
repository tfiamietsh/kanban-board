import type { FC } from 'preact/compat'

const Home: FC = () => {
  return (
    <div class="mx-1 p-1 rounded-md bg-slate-950/50 text-center content-center h-16 text-lg">
      Hi, let's start using Kanban board with login
    </div>
  )
}

export default Home
